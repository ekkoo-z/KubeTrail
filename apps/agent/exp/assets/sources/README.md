# EXP Source Assets

Reusable reviewed source templates can be staged here. Generated EXP bundles should copy or synthesize source under `exp/generated/<run-id>/src` and keep build instructions in the bundle.
